# WorkTool AI 中枢系统 - 系统开发规划报告

> **文档版本**: v1.0  
> **创建时间**: 2026-02-10  
> **维护者**: WorkTool AI 团队  
> **基于文档**: 系统分析报告 × 3 + 业务流程说明

---

## 📋 目录

- [一、业务模型与核心目标](#一业务模型与核心目标)
- [二、角色体系规划](#二角色体系规划)
- [三、系统架构设计](#三系统架构设计)
- [四、功能模块规划](#四功能模块规划)
- [五、数据模型规划](#五数据模型规划)
- [六、技术方案规划](#六技术方案规划)
- [七、开发阶段规划](#七开发阶段规划)
- [八、风险与挑战](#八风险与挑战)

---

## 一、业务模型与核心目标

### 1.1 业务模型概述

#### 1.1.1 核心业务流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    视频号运营服务商业务流程                      │
└─────────────────────────────────────────────────────────────────┘

前端转化客服
    │
    ▼
引导用户搭建视频号
    │
    ▼
视频号搭建完成
    │
    ▼
分配给运营团队（老板）
    │
    ▼
社群运营 & 售后服务（最少7天满意度维护）
    │
    ├─▶ 视频号运营问题处理
    ├─▶ 实名认证协助
    ├─▶ 任务配合提醒
    ├─▶ 满意度维护
    └─▶ 数据分析报告
```

#### 1.1.2 核心价值

| 价值维度 | 说明 | 技术实现 |
|---------|------|----------|
| **自动化运营** | 机器人7x24小时监控和回复 | 多机器人轮换、AI自动回复 |
| **智能协同** | 人工与机器人无缝配合 | 工作人员识别、协同决策 |
| **售后保障** | 及时发现和处理售后问题 | 任务跟踪、满意度分析 |
| **满意度管理** | 7天满意度维护和评估 | 数据分析、报告生成 |
| **数据驱动** | 基于数据优化运营策略 | 数据分析、腾讯文档对接 |

### 1.2 核心目标

#### 1.2.1 短期目标（1-3个月）
- ✅ 完善机器人角色体系（4类机器人）
- ✅ 实现工作流程引擎（基于流程引擎）
- ✅ 完善上下文管理系统
- ✅ 实现售后任务管理系统
- ✅ 实现用户和社群会话管理

#### 1.2.2 中期目标（3-6个月）
- ✅ 实现多AI模型协同（意图识别+回复生成+图片识别）
- ✅ 完善告警升级机制
- ✅ 实现满意度分析系统
- ✅ 实现数据分析报告系统
- ✅ 对接腾讯文档实时输出

#### 1.2.3 长期目标（6-12个月）
- ✅ 实现完全自动化运营（晚7点后）
- ✅ 实现智能调度系统
- ✅ 实现预测性运营（提前预判问题）
- ✅ 实现多机器人协同决策
- ✅ 实现运营效果评估体系

---

## 二、角色体系规划

### 2.1 机器人角色体系

#### 2.1.1 机器人类型定义

| 机器人类型 | 时段 | 职责 | 权限 |
|-----------|------|------|------|
| **监控机器人** | 24小时 | 监控所有消息，不回复 | 只读 |
| **通知机器人** | 24小时 | 发送通知、提醒、告警 | 发送权限 |
| **白班回复机器人** | 早9点-晚9点 | 社群运营消息回复、疑虑解答 | 发送权限 |
| **晚班回复机器人** | 晚9点-次日9点 | 社群运营消息回复、疑虑解答 | 发送权限 |

#### 2.1.2 机器人调度策略

```
┌─────────────────────────────────────────────────────────────────┐
│                    机器人调度策略                                │
└─────────────────────────────────────────────────────────────────┘

时间段划分：
├─ 白天时段（09:00 - 21:00）
│  └─ 优先使用白班回复机器人
│     └─ 辅助通知机器人
│
├─ 晚间时段（21:00 - 次日09:00）
│  └─ 优先使用晚班回复机器人
│     └─ 辅助通知机器人
│
└─ 紧急时段（全天）
   └─ 监控机器人 + 通知机器人随时待命
```

#### 2.1.3 机器人职责边界

| 场景 | 使用机器人 | 说明 |
|------|-----------|------|
| 社群运营消息回复 | 白班/晚班回复机器人 | 疑虑解答、排除困惑 |
| 售后任务提醒 | 通知机器人 | 提醒用户配合 |
| 紧急告警通知 | 通知机器人 | P0级别告警 |
| 系统异常通知 | 通知机器人 | 系统故障通知 |
| 消息监控 | 监控机器人 | 监控所有消息，不回复 |
| 监控机器人消息 | 监控机器人 | 记录回复机器人的消息 |
| 运营老板消息 | 监控机器人 | 监控财神爷的消息 |

### 2.2 工作人员角色体系

#### 2.2.1 工作人员类型

| 工作人员类型 | 时段 | 职责 | 状态 |
|-------------|------|------|------|
| **售后A** | 早9点-晚7点 | 处理售后问题、任务分配 | 在线/忙碌/离线 |
| **售后B** | 午2点-晚11点 | 处理售后问题、任务分配 | 在线/忙碌/离线 |
| **群助理** | 早9点-晚7点 | 跟进状态、协助运营 | 在线/忙碌/离线 |

#### 2.2.2 工作人员识别规则

```typescript
interface StaffIdentifier {
  // 企业名称特征
  companyNames: string[];  // 例如：["WorkTool", "扣子", "Coze", "测试公司"]
  
  // 备注名特征
  remarkNames: string[];   // 例如：["售后A", "售后B", "群助理"]
  
  // 昵称特征
  nicknames: string[];     // 例如：["售后", "助理", "管理员"]
  
  // 特殊标识
  specialIds: string[];    // 例如：["staff", "admin", "support"]
  
  // 群组特征
  groupNames: string[];    // 例如：["内部群", "管理群", "运营群"]
}
```

#### 2.2.3 工作人员状态管理

| 状态 | 说明 | 触发条件 |
|------|------|---------|
| **在线** | 工作人员在线，可以处理任务 | 最近5分钟内有活动 |
| **忙碌** | 工作人员正在处理任务 | 当前有未完成的任务 |
| **离线** | 工作人员不在线 | 最近30分钟内无活动 |

### 2.3 用户角色体系

#### 2.3.1 用户类型

| 用户类型 | 说明 | 特点 |
|---------|------|------|
| **号主（用户）** | 接受视频号运营服务的客户 | 7天满意度维护 |
| **运营（老板）** | 第三方运营团队 | 财神爷，需特殊维护 |
| **潜在客户** | 未加入视频号运营的用户 | 需要转化 |

#### 2.3.2 用户画像

```typescript
interface UserProfile {
  // 基本信息
  userId: string;
  userName: string;
  companyName?: string;
  enterpriseName?: string;
  
  // 会话信息
  sessionType: 'user' | 'group';
  messageCount: number;
  lastMessageTime: string;
  joinedAt: string;
  
  // 满意度
  satisfactionScore: number;  // 0-100
  problemResolutionRate: number;  // 0-1
  
  // 任务状态
  hasPendingTask: boolean;
  taskId?: string;
  taskType?: string;
  taskStatus?: string;
  
  // 历史记录
  isNewUser: boolean;
  totalSessions: number;
  totalMessages: number;
}
```

---

## 三、系统架构设计

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    WorkTool AI 中枢系统架构                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐     ┌──────────────────┐                 │
│  │   企业微信平台   │     │   管理后台前端   │                 │
│  │                  │     │                  │                 │
│  │ • 4类机器人      │◀────┤ • 流程引擎管理  │                 │
│  │ • 工作人员       │     │ • 角色管理      │                 │
│  │ • 号主/运营      │     │ • 会话管理      │                 │
│  │ • 社群消息       │     │ • 售后任务管理  │                 │
│  └────────┬─────────┘     └────────┬─────────┘                 │
│           │                       │                            │
│           │ Webhook               │ HTTP API                  │
│           │                       │                            │
│           ▼                       ▼                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                  后端服务 (Node.js)                        │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │   │
│  │  │ 回调接收层   │  │ 流程引擎层   │  │ AI 服务层   │  │   │
│  │  │              │  │              │  │              │  │   │
│  │  │ • 消息回调   │  │ • 工作流执行 │  │ • 意图识别   │  │   │
│  │  │ • 图片识别   │  │ • 节点调度   │  │ • 回复生成   │  │   │
│  │  │ • 结果回调   │  │ • 协同决策   │  │ • 图片OCR    │  │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  │   │
│  │                                                         │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │   │
│  │  │ 角色管理层   │  │ 会话管理层   │  │ 售后管理层   │  │   │
│  │  │              │  │              │  │              │  │   │
│  │  │ • 机器人调度 │  │ • 用户会话   │  │ • 任务跟踪   │  │   │
│  │  │ • 工作人员   │  │ • 社群会话   │  │ • 满意度分析│  │   │
│  │  │ • 状态管理   │  │ • 上下文管理 │  │ • 进度监控   │  │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│           │                       │                            │
│           ▼                       ▼                            │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐   │
│  │ PostgreSQL   │     │    Redis     │     │  Object Store│   │
│  │              │     │              │     │   (OSS/S3)   │   │
│  │ • 会话数据   │     │ • 缓存       │     │ • 图片存储   │   │
│  │ • 消息记录   │     │ • 队列       │     │ • 文档存储   │   │
│  │ • 角色数据   │     │ • 状态       │     │              │   │
│  └──────────────┘     └──────────────┘     └──────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 核心流程架构

#### 3.2.1 消息处理流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    消息处理核心流程                              │
└─────────────────────────────────────────────────────────────────┘

用户发送消息
    │
    ▼
机器人检测并上报
    │
    ▼ [回调接收层]
接收消息回调
    │
    ├─▶ 消息类型判断
    │   ├─ 文本消息 → 流程A
    │   └─ 图片消息 → 流程B
    │
    ▼
[流程A：文本消息]
    │
    ├─▶ 1. 消息保存
    │       ├─ 保存到 session_messages
    │       ├─ 提取用户信息
    │       └─ 提取群聊信息
    │
    ├─▶ 2. 图片识别（如果有图片）
    │       ├─ OCR识别
    │       └─ 提取文本内容
    │
    ├─▶ 3. 工作人员识别
    │       ├─ 判断是否为工作人员
    │       ├─ 记录工作人员状态
    │       └─ 标记工作人员消息
    │
    ├─▶ 4. 推送到监控
    │       ├─ 业务消息监控
    │       └─ AI交互监控
    │
    ├─▶ 5. 协同分析
    │       ├─ 判断是否需要介入
    │       ├─ 工作人员活动分析
    │       └─ 满意度分析
    │
    ├─▶ [分支：需要介入]
    │       │
    │       ├─▶ 6. 意图判断
    │       │       ├─ AI意图识别
    │       │       └─ 意图分类
    │       │
    │       ├─▶ 7. 告警分类
    │       │       ├─ 判断是否需要告警
    │       │       ├─ 告警级别分类
    │       │       └─ 告警策略选择
    │       │
    │       ├─▶ 8. AI回复生成
    │       │       ├─ 结合上下文
    │       │       ├─ 生成回复内容
    │       │       └─ 回复策略选择
    │       │
    │       ├─▶ 9. 通讯执行
    │       │       ├─ 选择机器人
    │       │       ├─ 构建指令
    │       │       └─ 发送指令
    │       │
    │       └─▶ 10. 结果确认
    │               ├─ 接收回执
    │               ├─ 更新会话状态
    │               └─ 记录发送结果
    │
    └─▶ [分支：不需要介入]
            │
            ├─▶ 11. 会话保存
            │       ├─ 更新会话状态
            │       └─ 记录消息
            │
            └─▶ 12. 持续监控
                    ├─ 监控后续消息
                    └─ 更新满意度
```

#### 3.2.2 售后任务流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    售后任务管理流程                              │
└─────────────────────────────────────────────────────────────────┘

售后发现问题
    │
    ▼
创建售后任务
    │
    ├─▶ 任务类型判断
    │   ├─ 扫码认证
    │   ├─ 绑定手机号
    │   ├─ 实名认证
    │   ├─ 发送自拍
    │   └─ 其他配合任务
    │
    ▼
分配给售后人员
    │
    ├─▶ 选择可用售后
    │   ├─ 检查在线状态
    │   ├─ 检查忙碌状态
    │   └─ 负载均衡
    │
    ▼
通知号主配合
    │
    ├─▶ 机器人发送消息
    │   ├─ 选择通知机器人
    │   ├─ 构建@消息
    │   └─ 发送到群聊
    │
    ▼
等待号主响应
    │
    ├─▶ 超时检测
    │   ├─ 检测号主是否响应
    │   ├─ 超时提醒
    │   └─ 通知售后介入
    │
    ▼
号主响应
    │
    ├─▶ 检测号主响应
    │   ├─ "在" → 任务进行中
    │   ├─ "好" → 任务进行中
    │   └─ 其他 → 可能需要引导
    │
    ▼
售后跟进
    │
    ├─▶ 检测售后响应
    │   ├─ 检测售后是否回复
    │   ├─ 超时提醒
    │   └─ 记录跟进情况
    │
    ▼
任务完成
    │
    ├─▶ 验证任务完成
    │   ├─ 确认号主配合完成
    │   ├─ 更新任务状态
    │   └─ 记录完成时间
    │
    ▼
满意度评估
    │
    ├─▶ 收集反馈
    │   ├─ 号主满意度
    │   ├─ 响应时间
    │   └─ 处理质量
    │
    ▼
生成报告
    │
    ├─▶ 每日报告
    │   ├─ 任务完成率
    │   ├─ 满意度评分
    │   ├─ 平均响应时间
    │   └─ 改进建议
    │
    └─▶ 同步到腾讯文档
            ├─ 实时输出进度
            └─ 生成可视化报告
```

---

## 四、功能模块规划

### 4.1 核心功能模块

#### 4.1.1 角色管理模块

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 机器人角色管理 | 管理4类机器人的配置、状态、权限 | P0 |
| 工作人员管理 | 管理售后人员、群助理的配置、状态 | P0 |
| 机器人调度系统 | 根据时段自动调度机器人 | P0 |
| 工作人员状态监控 | 实时监控工作人员在线/忙碌/离线状态 | P0 |
| 角色权限控制 | 控制不同角色的操作权限 | P1 |

#### 4.1.2 流程引擎模块

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 工作流可视化 | 可视化编辑工作流 | P0 |
| 节点类型扩展 | 扩展消息处理节点、售后任务节点 | P0 |
| 协同决策节点 | 实现人工介入判断逻辑 | P0 |
| 意图判断节点 | 实现AI意图识别 | P0 |
| 告警分类节点 | 实现告警分级和策略 | P0 |
| AI回复节点 | 实现AI回复生成 | P0 |
| 通讯执行节点 | 实现机器人指令发送 | P0 |
| 结果确认节点 | 实现回执处理 | P0 |
| 工作流版本管理 | 支持工作流版本管理 | P1 |

#### 4.1.3 上下文管理模块

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 用户会话管理 | 按用户创建和管理会话 | P0 |
| 社群会话管理 | 按群聊创建和管理会话 | P0 |
| 历史消息检索 | 检索最近N条历史消息 | P0 |
| 用户画像管理 | 管理用户满意度、历史记录 | P0 |
| 上下文数据结构 | 统一的上下文数据格式 | P0 |
| 会话状态管理 | 活跃/空闲/沉睡/归档 | P1 |

#### 4.1.4 售后任务管理模块

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 任务创建 | 创建售后任务 | P0 |
| 任务分配 | 自动分配给可用售后 | P0 |
| 任务跟踪 | 实时跟踪任务进度 | P0 |
| 超时提醒 | 超时自动提醒 | P0 |
| 任务完成验证 | 验证任务是否完成 | P0 |
| 满意度评估 | 评估号主满意度 | P0 |
| 日报生成 | 生成每日报告 | P0 |
| 腾讯文档对接 | 实时同步到腾讯文档 | P1 |

#### 4.1.5 AI服务模块

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 意图识别AI | 识别用户意图 | P0 |
| 回复生成AI | 生成回复内容 | P0 |
| 图片识别AI | OCR识别图片内容 | P0 |
| 满意度分析AI | 分析用户满意度 | P1 |
| 协同决策AI | 判断是否需要介入 | P1 |
| AI模型切换 | 支持多模型切换 | P2 |

#### 4.1.6 告警管理模块

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 告警规则配置 | 配置告警规则 | P0 |
| 告警级别分类 | P0/P1/P2/P3 | P0 |
| 告警升级机制 | 自动升级告警 | P0 |
| 告警通知 | 选择机器人发送告警 | P0 |
| 告警面板 | 展示告警信息 | P0 |
| 告警历史 | 记录告警历史 | P1 |
| 告警统计 | 统计告警数据 | P1 |

#### 4.1.7 监控管理模块

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 业务消息监控 | 展示所有业务消息 | P0 |
| AI交互监控 | 展示AI交互情况 | P0 |
| 售后实时监控大屏 | 实时监控售后任务 | P0 |
| 机器人状态监控 | 监控机器人在线状态 | P0 |
| 工作人员状态监控 | 监控工作人员状态 | P0 |
| 用户满意度监控 | 监控用户满意度 | P1 |

#### 4.1.8 数据分析模块

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 满意度分析 | 分析用户满意度 | P1 |
| 活跃度分析 | 分析用户活跃度 | P1 |
| 协同效果分析 | 分析人工+机器人协同效果 | P1 |
| 运营效果评估 | 评估运营团队效果 | P1 |
| 每日报告生成 | 生成每日数据报告 | P1 |
| 改进建议生成 | 基于数据生成改进建议 | P2 |

---

## 五、数据模型规划

### 5.1 核心数据表

#### 5.1.1 机器人角色表（robot_roles）

```sql
CREATE TABLE robot_roles (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  role_type VARCHAR(50) NOT NULL UNIQUE,  -- 'monitor', 'notification', 'day_shift', 'night_shift'
  role_name VARCHAR(100) NOT NULL,         -- '监控机器人', '通知机器人', '白班回复机器人', '晚班回复机器人'
  description TEXT,
  
  -- 时段配置
  time_start VARCHAR(10),                 -- '09:00'
  time_end VARCHAR(10),                   -- '21:00'
  
  -- 权限配置
  can_read BOOLEAN DEFAULT true,
  can_send BOOLEAN DEFAULT false,
  can_monitor BOOLEAN DEFAULT false,
  
  -- 优先级
  priority INTEGER DEFAULT 5,
  
  -- 状态
  is_active BOOLEAN DEFAULT true,
  
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

#### 5.1.2 机器人配置表（robot_configs）

```sql
CREATE TABLE robot_configs (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  robot_id VARCHAR(64) NOT NULL UNIQUE,
  robot_name VARCHAR(255) NOT NULL,
  nickname VARCHAR(255),
  
  -- 角色关联
  role_id VARCHAR(36) REFERENCES robot_roles(id),
  
  -- 机器人信息
  api_base_url VARCHAR(500),
  status VARCHAR(20) DEFAULT 'offline',
  is_active BOOLEAN DEFAULT true,
  
  -- 企业信息
  company VARCHAR(255),
  enterprise_name VARCHAR(255),
  
  -- 时段配置（覆盖角色配置）
  time_start VARCHAR(10),
  time_end VARCHAR(10),
  
  -- 回调地址
  message_callback_url VARCHAR(500),
  result_callback_url VARCHAR(500),
  status_callback_url VARCHAR(500),
  
  -- 调度配置
  auto_schedule BOOLEAN DEFAULT true,
  max_concurrent_tasks INTEGER DEFAULT 10,
  
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

#### 5.1.3 工作人员表（staff_members）

```sql
CREATE TABLE staff_members (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  staff_id VARCHAR(64) NOT NULL UNIQUE,
  staff_name VARCHAR(100) NOT NULL,
  staff_type VARCHAR(50),                  -- 'after_sales', 'group_assistant'
  
  -- 企业信息
  company_name VARCHAR(255),
  enterprise_name VARCHAR(255),
  
  -- 时段配置
  time_start VARCHAR(10),                 -- '09:00'
  time_end VARCHAR(10),                   -- '19:00'
  
  -- 状态
  status VARCHAR(20) DEFAULT 'offline',   -- 'online', 'busy', 'offline'
  last_active_time TIMESTAMP WITH TIME ZONE,
  
  -- 负载
  current_tasks INTEGER DEFAULT 0,
  max_tasks INTEGER DEFAULT 5,
  
  -- 联系方式
  notification_robot_id VARCHAR(64),     -- 通知机器人ID
  notification_method VARCHAR(50),        -- 'robot', 'email', 'sms'
  
  is_active BOOLEAN DEFAULT true,
  
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

#### 5.1.4 用户会话表（user_sessions）

```sql
CREATE TABLE user_sessions (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id VARCHAR(255) NOT NULL UNIQUE,
  
  -- 用户信息
  user_id VARCHAR(255),
  user_name VARCHAR(255),
  company_name VARCHAR(255),
  enterprise_name VARCHAR(255),
  
  -- 会话信息
  session_type VARCHAR(20) DEFAULT 'user', -- 'user', 'group'
  message_count INTEGER DEFAULT 0,
  last_message_time TIMESTAMP WITH TIME ZONE,
  joined_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  
  -- 满意度
  satisfaction_score INTEGER DEFAULT 50,   -- 0-100
  problem_resolution_rate DECIMAL(5, 2) DEFAULT 0.00,  -- 0-1
  
  -- 历史记录
  is_new_user BOOLEAN DEFAULT true,
  total_sessions INTEGER DEFAULT 0,
  total_messages INTEGER DEFAULT 0,
  
  -- 状态
  status VARCHAR(20) DEFAULT 'active',    -- 'active', 'idle', 'inactive', 'archived'
  
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

#### 5.1.5 社群会话表（group_sessions）

```sql
CREATE TABLE group_sessions (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id VARCHAR(255) NOT NULL UNIQUE,
  
  -- 群聊信息
  group_id VARCHAR(255),
  group_name VARCHAR(255),
  member_count INTEGER DEFAULT 0,
  
  -- 会话信息
  message_count INTEGER DEFAULT 0,
  last_message_time TIMESTAMP WITH TIME ZONE,
  joined_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  
  -- 成员活跃度
  active_member_count INTEGER DEFAULT 0,
  inactive_member_count INTEGER DEFAULT 0,
  
  -- 售后任务统计
  pending_task_count INTEGER DEFAULT 0,
  completed_task_count INTEGER DEFAULT 0,
  
  -- 运营信息
  operator_id VARCHAR(255),
  operator_name VARCHAR(255),
  
  -- 状态
  status VARCHAR(20) DEFAULT 'active',
  
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

#### 5.1.6 售后任务表（after_sales_tasks）

```sql
CREATE TABLE after_sales_tasks (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  task_id VARCHAR(64) NOT NULL UNIQUE,
  
  -- 关联信息
  session_id VARCHAR(255),
  user_id VARCHAR(255),
  user_name VARCHAR(255),
  group_id VARCHAR(255),
  group_name VARCHAR(255),
  robot_id VARCHAR(64),
  
  -- 任务信息
  task_type VARCHAR(50) NOT NULL,          -- 'scan_qrcode', 'bind_phone', 'real_name', 'send_selfie', 'other'
  task_name VARCHAR(255),
  task_description TEXT,
  
  -- 分配信息
  staff_id VARCHAR(36) REFERENCES staff_members(id),
  staff_name VARCHAR(255),
  assigned_at TIMESTAMP WITH TIME ZONE,
  
  -- 任务状态
  status VARCHAR(50) DEFAULT 'pending',   -- 'pending', 'waiting_user', 'in_progress', 'completed', 'failed'
  
  -- 时间节点
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  user_response_at TIMESTAMP WITH TIME ZONE,
  staff_response_at TIMESTAMP WITH TIME ZONE,
  completed_at TIMESTAMP WITH TIME ZONE,
  
  -- 超时配置
  user_response_timeout INTEGER DEFAULT 3600,  -- 秒
  staff_response_timeout INTEGER DEFAULT 600,
  
  -- 满意度
  satisfaction_score INTEGER,
  response_time INTEGER,                  -- 秒
  
  -- 备注
  notes TEXT,
  
  is_active BOOLEAN DEFAULT true,
  
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

#### 5.1.7 上下文数据表（session_contexts）

```sql
CREATE TABLE session_contexts (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id VARCHAR(255) NOT NULL,
  
  -- 上下文类型
  context_type VARCHAR(20) NOT NULL,     -- 'user', 'group'
  
  -- 历史消息（JSON）
  history_messages JSONB DEFAULT '[]',
  history_count INTEGER DEFAULT 0,
  
  -- 用户画像
  user_profile JSONB DEFAULT '{}',
  
  -- 群聊画像
  group_profile JSONB DEFAULT '{}',
  
  -- 工作人员状态
  staff_status JSONB DEFAULT '{}',
  
  -- 任务状态
  task_status JSONB DEFAULT '{}',
  
  -- 缓存时间
  cached_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  expires_at TIMESTAMP WITH TIME ZONE,
  
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

---

## 六、技术方案规划

### 6.1 AI服务方案

#### 6.1.1 AI模型选择

| 功能 | 推荐模型 | 说明 |
|------|---------|------|
| 意图识别 | DeepSeek-V3 | 精准识别用户意图 |
| 回复生成 | Kimi-moonshot | 生成高质量回复 |
| 图片识别 | OCR服务 + Vision模型 | 提取图片内容 |
| 满意度分析 | 自定义模型 | 基于历史数据分析 |
| 协同决策 | 规则引擎 + AI | 规则为主，AI辅助 |

#### 6.1.2 AI交互机制

```
┌─────────────────────────────────────────────────────────────────┐
│                    AI交互机制                                    │
└─────────────────────────────────────────────────────────────────┘

系统准备上下文
    │
    ├─ 判断会话类型（新会话/老会话）
    ├─ 检索历史消息（老会话：20条，新会话：空）
    ├─ 检索用户画像
    ├─ 检索工作人员状态
    ├─ 检索售后任务状态
    └─ 检索群聊信息
    │
    ▼
发送给AI
    │
    ├─ 当前消息
    ├─ 历史上下文
    ├─ 用户画像
    ├─ 工作人员状态
    ├─ 任务状态
    └─ 群聊信息
    │
    ▼
AI处理
    │
    ├─ 意图识别
    ├─ 情感分析
    ├─ 判断是否需要回复
    ├─ 判断是否需要告警
    ├─ 判断是否需要介入
    └─ 生成回复建议
    │
    ▼
AI返回
    │
    ├─ intent: 意图类型
    ├─ confidence: 置信度
    ├─ sentiment: 情感倾向
    ├─ need_reply: 是否需要回复
    ├─ reply_suggestion: 回复建议
    ├─ need_alert: 是否需要告警
    ├─ need_intervention: 是否需要介入
    └─ context_analysis: 上下文分析
```

### 6.2 腾讯文档对接方案

#### 6.2.1 对接方式

| 功能 | 对接方式 | 说明 |
|------|---------|------|
| 售后任务进度 | 实时同步 | 每次任务状态变更时同步 |
| 每日报告 | 定时同步 | 每天凌晨2点生成并同步 |
| 数据统计 | 按需同步 | 前端请求时实时同步 |

#### 6.2.2 数据格式

```typescript
interface TencentDocData {
  // 售后任务进度
  tasks: {
    taskId: string;
    userId: string;
    userName: string;
    groupName: string;
    taskType: string;
    status: string;
    assignedStaff: string;
    createdAt: string;
    completedAt?: string;
    satisfactionScore?: number;
  }[];
  
  // 每日统计
  dailyStats: {
    date: string;
    totalTasks: number;
    completedTasks: number;
    pendingTasks: number;
    avgResponseTime: number;
    avgSatisfaction: number;
  };
  
  // 改进建议
  suggestions: {
    type: string;
    description: string;
    priority: string;
  }[];
}
```

### 6.3 机器人调度方案

#### 6.3.1 调度策略

```typescript
interface RobotScheduleStrategy {
  // 白天时段（09:00 - 21:00）
  dayTime: {
    primary: 'day_shift_robot',      // 白班回复机器人
    secondary: 'notification_robot',  // 通知机器人
    monitor: 'monitor_robot'          // 监控机器人
  };
  
  // 晚间时段（21:00 - 次日09:00）
  nightTime: {
    primary: 'night_shift_robot',     // 晚班回复机器人
    secondary: 'notification_robot',  // 通知机器人
    monitor: 'monitor_robot'          // 监控机器人
  };
  
  // 紧急情况
  emergency: {
    primary: 'notification_robot',   // 通知机器人
    secondary: 'monitor_robot'        // 监控机器人
  };
}
```

#### 6.3.2 调度算法

```javascript
async function selectRobot(messageContext) {
  const currentTime = getCurrentTime();
  const currentHour = currentTime.getHours();
  
  // 判断时段
  if (currentHour >= 9 && currentHour < 21) {
    // 白天时段
    return await selectDayTimeRobot(messageContext);
  } else {
    // 晚间时段
    return await selectNightTimeRobot(messageContext);
  }
}

async function selectDayTimeRobot(messageContext) {
  // 判断消息类型
  if (messageContext.type === 'alert') {
    // 告警消息，使用通知机器人
    return getRobotByRole('notification');
  } else if (messageContext.type === 'monitor') {
    // 监控消息，使用监控机器人
    return getRobotByRole('monitor');
  } else {
    // 普通消息，使用白班回复机器人
    return getRobotByRole('day_shift');
  }
}
```

---

## 七、开发阶段规划

### 7.1 第一阶段：核心功能完善（1个月）

#### 7.1.1 目标
- ✅ 完善机器人角色体系
- ✅ 实现工作人员识别和管理
- ✅ 实现用户和社群会话管理
- ✅ 完善上下文管理系统

#### 7.1.2 任务清单

| 任务 | 优先级 | 预估工作量 |
|------|--------|-----------|
| 设计并实现机器人角色表 | P0 | 2天 |
| 实现工作人员识别逻辑 | P0 | 3天 |
| 实现用户会话管理 | P0 | 3天 |
| 实现社群会话管理 | P0 | 3天 |
| 实现上下文检索机制 | P0 | 4天 |
| 实现机器人调度系统 | P0 | 4天 |
| 实现工作人员状态监控 | P0 | 3天 |
| 测试和优化 | P0 | 4天 |

### 7.2 第二阶段：流程引擎实现（2个月）

#### 7.2.1 目标
- ✅ 实现完整的工作流程引擎
- ✅ 实现消息处理工作流
- ✅ 实现售后任务工作流
- ✅ 实现AI协同工作流

#### 7.2.2 任务清单

| 任务 | 优先级 | 预估工作量 |
|------|--------|-----------|
| 设计消息处理工作流 | P0 | 5天 |
| 实现消息处理节点 | P0 | 7天 |
| 设计售后任务工作流 | P0 | 5天 |
| 实现售后任务节点 | P0 | 7天 |
| 实现协同决策节点 | P0 | 5天 |
| 实现意图判断节点 | P0 | 5天 |
| 实现告警分类节点 | P0 | 5天 |
| 实现AI回复节点 | P0 | 5天 |
| 实现通讯执行节点 | P0 | 5天 |
| 实现结果确认节点 | P0 | 5天 |
| 工作流可视化编辑 | P1 | 7天 |
| 测试和优化 | P0 | 7天 |

### 7.3 第三阶段：AI服务优化（1.5个月）

#### 7.3.1 目标
- ✅ 优化AI意图识别
- ✅ 优化AI回复生成
- ✅ 实现图片识别功能
- ✅ 实现满意度分析

#### 7.3.2 任务清单

| 任务 | 优先级 | 预估工作量 |
|------|--------|-----------|
| 优化意图识别AI | P0 | 7天 |
| 优化回复生成AI | P0 | 7天 |
| 集成图片识别服务 | P0 | 5天 |
| 实现满意度分析AI | P1 | 7天 |
| 实现协同决策AI | P1 | 7天 |
| AI模型切换功能 | P2 | 5天 |
| 测试和优化 | P0 | 5天 |

### 7.4 第四阶段：售后任务管理（1.5个月）

#### 7.4.1 目标
- ✅ 实现售后任务管理系统
- ✅ 实现任务跟踪和监控
- ✅ 实现超时提醒
- ✅ 实现满意度评估

#### 7.4.2 任务清单

| 任务 | 优先级 | 预估工作量 |
|------|--------|-----------|
| 设计售后任务表 | P0 | 3天 |
| 实现任务创建功能 | P0 | 5天 |
| 实现任务分配功能 | P0 | 5天 |
| 实现任务跟踪功能 | P0 | 5天 |
| 实现超时提醒功能 | P0 | 5天 |
| 实现任务完成验证 | P0 | 5天 |
| 实现满意度评估 | P1 | 7天 |
| 实现日报生成 | P1 | 7天 |
| 测试和优化 | P0 | 5天 |

### 7.5 第五阶段：监控和数据分析（1个月）

#### 7.5.1 目标
- ✅ 实现售后实时监控大屏
- ✅ 实现数据分析系统
- ✅ 实现报告生成系统
- ✅ 对接腾讯文档

#### 7.5.2 任务清单

| 任务 | 优先级 | 预估工作量 |
|------|--------|-----------|
| 设计售后监控大屏 | P0 | 5天 |
| 实现监控大屏前端 | P0 | 7天 |
| 实现数据分析功能 | P1 | 7天 |
| 实现报告生成功能 | P1 | 5天 |
| 对接腾讯文档API | P1 | 5天 |
| 测试和优化 | P0 | 5天 |

---

## 八、风险与挑战

### 8.1 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|---------|
| AI模型性能不稳定 | 高 | 中 | 多模型备选、降级策略 |
| 机器人API不稳定 | 高 | 中 | 重试机制、熔断器 |
| 数据库性能瓶颈 | 中 | 低 | 缓存优化、分库分表 |
| 并发处理能力不足 | 中 | 低 | 异步处理、队列管理 |

### 8.2 业务风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|---------|
| 售后任务处理不及时 | 高 | 中 | 超时提醒、自动升级 |
| 用户满意度下降 | 高 | 中 | 及时介入、数据分析 |
| 运营团队配合度低 | 中 | 中 | 特殊处理、人工介入 |
| 系统故障影响业务 | 高 | 低 | 熔断器、降级策略 |

### 8.3 挑战与建议

#### 8.3.1 技术挑战

1. **多机器人协同**
   - 挑战：如何确保多机器人之间的协同一致性
   - 建议：统一调度系统、状态共享、锁机制

2. **上下文管理**
   - 挑战：如何高效检索和管理大量历史消息
   - 建议：缓存优化、分页加载、索引优化

3. **AI响应质量**
   - 挑战：如何确保AI回复的准确性和合适性
   - 建议：多模型对比、人工审核、持续优化

#### 8.3.2 业务挑战

1. **满意度管理**
   - 挑战：如何准确评估用户满意度
   - 建议：多维度评估、及时收集反馈、数据分析

2. **售后任务管理**
   - 挑战：如何确保售后任务及时处理
   - 建议：超时提醒、自动升级、实时监控

3. **运营团队配合**
   - 挑战：如何提高运营团队的配合度
   - 建议：提供数据支持、及时反馈、培训指导

---

## 附录

### A. 相关文档

- [WorkTool AI 中枢系统完整分析报告](./WorkTool_AI_中枢系统完整分析报告.md)
- [消息中心与上下文管理完整指南](./消息中心与上下文管理完整指南.md)
- [机器人通讯系统完整分析报告](./机器人通讯系统完整分析报告.md)
- [系统升级执行报告](./系统升级执行报告.md)

### B. 术语表

| 术语 | 说明 |
|------|------|
| 号主 | 接受视频号运营服务的用户 |
| 运营 | 第三方运营团队，老板 |
| 售后 | 处理售后问题的工作人员 |
| 群助理 | 跟进状态、协助运营的工作人员 |
| 监控机器人 | 监控所有消息，不回复 |
| 通知机器人 | 发送通知、提醒、告警 |
| 白班回复机器人 | 早9点-晚9点，社群运营消息回复 |
| 晚班回复机器人 | 晚9点-次日9点，社群运营消息回复 |

---

**文档结束**

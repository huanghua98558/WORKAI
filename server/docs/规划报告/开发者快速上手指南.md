# WorkTool AI 中枢系统 - 开发者快速上手指南

## 📋 目录

1. [开发环境准备](#1-开发环境准备)
2. [项目结构](#2-项目结构)
3. [开发规范](#3-开发规范)
4. [常用命令](#4-常用命令)
5. [开发流程](#5-开发流程)
6. [调试技巧](#6-调试技巧)
7. [常见问题](#7-常见问题)

---

## 1. 开发环境准备

### 1.1 系统要求

```
操作系统：
- macOS 10.15+
- Ubuntu 20.04+
- Windows 10+ (WSL2推荐)

必备软件：
- Node.js 18+
- pnpm 8+
- PostgreSQL 15+
- Redis 7+
- Git 2.30+

推荐工具：
- VS Code
- Postman
- pgAdmin
- Redis Commander
```

### 1.2 安装步骤

#### 步骤1：安装Node.js和pnpm

```bash
# 安装Node.js (使用nvm)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18

# 安装pnpm
npm install -g pnpm
```

#### 步骤2：安装PostgreSQL

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install postgresql-15 postgresql-contrib-15

# macOS
brew install postgresql@15

# Windows
# 从官网下载安装包
# https://www.postgresql.org/download/windows/
```

#### 步骤3：安装Redis

```bash
# Ubuntu/Debian
sudo apt-get install redis-server

# macOS
brew install redis

# Windows
# 下载Redis for Windows
# https://github.com/microsoftarchive/redis/releases
```

#### 步骤4：克隆项目

```bash
# 克隆项目仓库
git clone https://github.com/company/worktool-ai.git
cd worktool-ai

# 安装依赖
pnpm install
```

#### 步骤5：配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑环境变量
vim .env

# 配置以下内容：
DATABASE_URL="postgresql://worktool:password@localhost:5432/worktool_ai"
REDIS_URL="redis://localhost:6379"
AI_API_KEY="your-ai-api-key"
JWT_SECRET="your-jwt-secret"
```

#### 步骤6：初始化数据库

```bash
# 创建数据库
createdb worktool_ai

# 运行数据库迁移
pnpm db:migrate

# 填充测试数据（可选）
pnpm db:seed
```

#### 步骤7：启动开发服务

```bash
# 启动后端服务
pnpm dev:server

# 启动前端服务（新终端）
pnpm dev:client

# 或者同时启动前后端
pnpm dev
```

### 1.3 验证安装

```bash
# 检查服务状态
curl http://localhost:5000/health

# 预期响应：
{
  "status": "healthy",
  "timestamp": "2024-01-10T10:00:00Z",
  "checks": {
    "database": "ok",
    "redis": "ok",
    "ai_service": "ok"
  }
}

# 测试API
curl http://localhost:5000/api/v1/sessions
```

---

## 2. 项目结构

### 2.1 目录结构

```
worktool-ai/
├── server/                      # 后端服务
│   ├── src/
│   │   ├── api/                # API路由
│   │   │   ├── routes/         # 路由定义
│   │   │   ├── schemas/        # 数据模型
│   │   │   └── handlers/       # 请求处理器
│   │   ├── services/           # 业务逻辑
│   │   │   ├── flow-engine/    # 流程引擎
│   │   │   ├── ai-analyzer/    # AI分析
│   │   │   ├── alert-manager/  # 告警管理
│   │   │   └── task-manager/   # 任务管理
│   │   ├── models/             # 数据模型
│   │   ├── db/                 # 数据库连接
│   │   ├── utils/              # 工具函数
│   │   ├── config/             # 配置文件
│   │   └── index.ts            # 入口文件
│   ├── package.json
│   └── tsconfig.json
│
├── client/                     # 前端应用
│   ├── src/
│   │   ├── app/                # Next.js App Router
│   │   ├── components/         # 组件
│   │   │   ├── ui/             # UI组件 (shadcn/ui)
│   │   │   ├── features/       # 功能组件
│   │   │   └── layouts/        # 布局组件
│   │   ├── lib/                # 工具库
│   │   ├── hooks/              # 自定义Hooks
│   │   ├── store/              # 状态管理
│   │   └── types/              # TypeScript类型
│   ├── package.json
│   └── tsconfig.json
│
├── shared/                     # 共享代码
│   ├── types/                  # 共享类型
│   ├── constants/              # 共享常量
│   └── utils/                  # 共享工具
│
├── docs/                       # 文档
│   └── 规划报告/               # 规划文档
│
├── scripts/                    # 脚本
│   ├── setup.sh                # 环境设置
│   ├── deploy.sh               # 部署脚本
│   └── backup.sh               # 备份脚本
│
├── tests/                      # 测试
│   ├── unit/                   # 单元测试
│   ├── integration/            # 集成测试
│   └── e2e/                    # E2E测试
│
├── .env.example                # 环境变量示例
├── .gitignore                  # Git忽略文件
├── docker-compose.yml          # Docker配置
├── package.json                # 根package.json
└── README.md                   # 项目说明
```

### 2.2 核心文件说明

```
server/src/index.ts
├── 初始化Fastify应用
├── 注册插件
├── 注册路由
├── 配置中间件
└── 启动服务

server/src/api/routes/
├── auth.routes.ts              # 认证路由
├── users.routes.ts             # 用户路由
├── sessions.routes.ts          # 会话路由
├── alerts.routes.ts            # 告警路由
├── tasks.routes.ts             # 任务路由
└── health.routes.ts            # 健康检查路由

client/src/app/
├── layout.tsx                  # 根布局
├── page.tsx                    # 首页
├── dashboard/                  # 仪表盘
├── sessions/                   # 会话管理
├── alerts/                     # 告警管理
├── tasks/                      # 任务管理
└── analytics/                  # 分析报告
```

---

## 3. 开发规范

### 3.1 代码风格

#### TypeScript规范

```typescript
// ✅ 好的实践
interface User {
  id: string;
  username: string;
  email: string;
  createdAt: Date;
}

async function getUserById(id: string): Promise<User | null> {
  const user = await db.users.findOne({ id });
  return user || null;
}

// ❌ 避免的做法
function getUser(id) {  // 缺少类型注解
  return db.users.findOne({ id });
}
```

#### 命名规范

```typescript
// 文件名：kebab-case
// user-service.ts
// session-manager.ts

// 类名：PascalCase
class UserService {}
class SessionManager {}

// 函数名：camelCase
function getUserById() {}
function createAlert() {}

// 常量：UPPER_SNAKE_CASE
const MAX_RETRIES = 3;
const DEFAULT_TIMEOUT = 5000;

// 接口：PascalCase，无I前缀
interface User {}
interface Session {}

// 类型：PascalCase
type UserRole = 'admin' | 'user' | 'staff';
```

#### 注释规范

```typescript
/**
 * 根据ID获取用户信息
 *
 * @param id - 用户ID
 * @returns 用户对象，如果不存在则返回null
 * @throws Error 如果数据库查询失败
 *
 * @example
 * const user = await getUserById('user-001');
 */
async function getUserById(id: string): Promise<User | null> {
  try {
    const user = await db.users.findOne({ id });
    return user || null;
  } catch (error) {
    throw new Error(`Failed to get user: ${error.message}`);
  }
}

// 单行注释用于解释复杂逻辑
const token = generateToken();  // 生成JWT令牌，有效期为7天
```

### 3.2 Git规范

#### 分支策略

```
main          # 主分支，始终保持稳定
└── develop   # 开发分支，集成新功能
    ├── feature/xxx      # 功能分支
    ├── bugfix/xxx       # 修复分支
    └── hotfix/xxx       # 紧急修复分支
```

#### 提交信息规范

```
格式：<type>(<scope>): <subject>

类型：
- feat: 新功能
- fix: Bug修复
- docs: 文档更新
- style: 代码格式（不影响功能）
- refactor: 重构
- test: 测试相关
- chore: 构建/工具相关

示例：
feat(auth): add JWT authentication
fix(session): resolve connection leak
docs(readme): update installation guide
style(api): format code with prettier
refactor(user): extract user service
```

#### Pull Request规范

```
标题：简短描述PR内容

描述：
## 变更内容
- 功能1
- 功能2

## 测试
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 手动测试通过

## 截图
（如有UI变更，添加截图）

## 相关Issue
Fixes #123
```

### 3.3 API规范

#### RESTful API设计

```
GET    /api/v1/users           # 获取用户列表
GET    /api/v1/users/{id}      # 获取用户详情
POST   /api/v1/users           # 创建用户
PUT    /api/v1/users/{id}      # 更新用户
DELETE /api/v1/users/{id}      # 删除用户

POST   /api/v1/users/{id}/reset-password  # 用户操作
```

#### 响应格式

```typescript
// 成功响应
{
  "success": true,
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": "2024-01-10T10:00:00Z"
}

// 错误响应
{
  "success": false,
  "code": 400,
  "message": "请求参数错误",
  "errors": [
    {
      "field": "username",
      "message": "用户名不能为空"
    }
  ],
  "timestamp": "2024-01-10T10:00:00Z"
}
```

---

## 4. 常用命令

### 4.1 依赖管理

```bash
# 安装依赖
pnpm install

# 安装生产依赖
pnpm add package-name

# 安装开发依赖
pnpm add -D package-name

# 移除依赖
pnpm remove package-name

# 更新依赖
pnpm update
```

### 4.2 开发命令

```bash
# 启动开发环境（前后端）
pnpm dev

# 启动后端服务
pnpm dev:server

# 启动前端服务
pnpm dev:client

# 构建生产版本
pnpm build

# 启动生产服务
pnpm start
```

### 4.3 数据库命令

```bash
# 运行数据库迁移
pnpm db:migrate

# 回滚数据库迁移
pnpm db:rollback

# 创建新的迁移文件
pnpm db:migrate:create migration-name

# 填充测试数据
pnpm db:seed

# 重置数据库
pnpm db:reset
```

### 4.4 测试命令

```bash
# 运行所有测试
pnpm test

# 运行单元测试
pnpm test:unit

# 运行集成测试
pnpm test:integration

# 运行E2E测试
pnpm test:e2e

# 生成测试覆盖率报告
pnpm test:coverage

# 监听模式运行测试
pnpm test:watch
```

### 4.5 代码质量

```bash
# 代码检查
pnpm lint

# 自动修复代码问题
pnpm lint:fix

# 类型检查
pnpm type-check

# 格式化代码
pnpm format

# 运行所有质量检查
pnpm check
```

---

## 5. 开发流程

### 5.1 新功能开发

```
1. 创建功能分支
   git checkout -b feature/new-feature

2. 开发功能
   - 编写代码
   - 编写测试
   - 更新文档

3. 运行测试
   pnpm test

4. 代码检查
   pnpm lint
   pnpm type-check

5. 提交代码
   git add .
   git commit -m "feat: add new feature"

6. 推送到远程
   git push origin feature/new-feature

7. 创建Pull Request
   - 填写PR描述
   - 关联Issue
   - 等待Code Review

8. 合并后删除分支
   git checkout develop
   git pull origin develop
   git branch -d feature/new-feature
```

### 5.2 Bug修复

```
1. 创建修复分支
   git checkout -b bugfix/fix-issue

2. 定位问题
   - 查看日志
   - 调试代码
   - 编写测试用例

3. 修复问题
   - 修改代码
   - 确保测试通过

4. 验证修复
   - 运行测试
   - 手动测试
   - 代码审查

5. 提交修复
   git commit -m "fix: resolve issue #123"

6. 创建PR并合并
```

### 5.3 代码审查

```markdown
## Code Review检查清单

- [ ] 代码符合项目规范
- [ ] 所有测试通过
- [ ] 无明显的性能问题
- [ ] 无安全漏洞
- [ ] 错误处理完善
- [ ] 代码注释清晰
- [ ] 更新了相关文档
- [ ] 向后兼容（如需要）
```

---

## 6. 调试技巧

### 6.1 后端调试

```typescript
// 使用console.log调试
console.log('User ID:', userId);
console.log('Session data:', JSON.stringify(session, null, 2));

// 使用debug模块
import Debug from 'debug';
const debug = Debug('worktool:auth');

debug('Authenticating user:', userId);

// 使用调试器（VS Code）
// 在需要调试的行设置断点
// 按F5启动调试
```

### 6.2 数据库调试

```sql
-- 查看慢查询
SELECT query, mean_exec_time, calls
FROM pg_stat_statements
ORDER BY mean_exec_time DESC
LIMIT 10;

-- 查看锁等待
SELECT * FROM pg_stat_activity WHERE wait_event_type = 'Lock';

-- 查看表大小
SELECT
  relname AS table_name,
  pg_size_pretty(pg_total_relation_size(relid)) AS size
FROM pg_catalog.pg_statio_user_tables
ORDER BY pg_total_relation_size(relid) DESC;
```

### 6.3 前端调试

```typescript
// 使用React DevTools
// 安装React DevTools浏览器插件

// 使用console调试
console.log('Component props:', props);
console.log('Component state:', state);

// 使用调试器
// 在VS Code中设置断点
// 按F5启动调试

// 查看网络请求
// 打开浏览器开发者工具
// 切换到Network标签
```

---

## 7. 常见问题

### 7.1 环境问题

**Q：pnpm install失败？**

A：清除缓存后重试
```bash
rm -rf node_modules
pnpm store prune
pnpm install
```

**Q：数据库连接失败？**

A：检查数据库服务是否启动
```bash
sudo systemctl status postgresql
sudo systemctl start postgresql
```

**Q：Redis连接失败？**

A：检查Redis服务是否启动
```bash
sudo systemctl status redis
sudo systemctl start redis
```

### 7.2 开发问题

**Q：热更新不工作？**

A：检查端口是否被占用
```bash
lsof -i:5000
kill -9 <PID>
pnpm dev
```

**Q：TypeScript类型错误？**

A：运行类型检查
```bash
pnpm type-check
```

**Q：测试失败？**

A：查看详细错误信息
```bash
pnpm test --verbose
```

### 7.3 部署问题

**Q：构建失败？**

A：检查环境变量配置
```bash
cat .env
```

**Q：服务启动失败？**

A：查看日志
```bash
journalctl -u worktool-api -n 100
tail -100 /var/log/worktool/api-error.log
```

---

## 8. 资源链接

### 8.1 官方文档

- [Next.js文档](https://nextjs.org/docs)
- [Fastify文档](https://www.fastify.io/docs/latest/)
- [PostgreSQL文档](https://www.postgresql.org/docs/)
- [Redis文档](https://redis.io/documentation)
- [TypeScript文档](https://www.typescriptlang.org/docs/)

### 8.2 工具文档

- [pnpm文档](https://pnpm.io/)
- [shadcn/ui文档](https://ui.shadcn.com/)
- [Drizzle ORM文档](https://orm.drizzle.team/)

### 8.3 团队资源

- 项目Wiki：https://wiki.company.com/worktool-ai
- 问题追踪：https://issues.company.com/worktool-ai
- 代码仓库：https://github.com/company/worktool-ai

---

**文档版本**：v1.0

**最后更新**：2024-01-10

**文档作者**：WorkTool AI 团队

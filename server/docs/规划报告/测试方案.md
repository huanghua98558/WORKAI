# WorkTool AI 中枢系统 - 测试方案

## 📋 目录

1. [测试概述](#1-测试概述)
2. [测试策略](#2-测试策略)
3. [测试用例](#3-测试用例)
4. [测试执行](#4-测试执行)
5. [测试报告](#5-测试报告)

---

## 1. 测试概述

### 1.1 测试目标

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              测试目标                                        │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  1. 功能测试                                                           │  │
│  │  - 验证所有功能是否正常工作                                            │  │
│  │  - 验证业务逻辑是否正确                                                │  │
│  │  - 验证边界条件处理                                                    │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  2. 性能测试                                                           │  │
│  │  - 验证系统响应时间                                                    │  │
│  │  - 验证系统并发能力                                                    │  │
│  │  - 验证系统稳定性                                                      │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  3. 安全测试                                                           │  │
│  │  - 验证权限控制是否有效                                                │  │
│  │  - 验证数据传输是否加密                                                │  │
│  │  - 验证输入验证是否完善                                                │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  4. 集成测试                                                           │  │
│  │  - 验证模块间集成是否正常                                              │  │
│  │  - 验证第三方服务集成是否正常                                           │  │
│  │  - 验证数据流转是否正确                                                │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 测试范围

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              测试范围                                        │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  后端测试                                                             │  │
│  │  - 用户管理API                                                         │  │
│  │  - 角色权限API                                                         │  │
│  │  - 会话管理API                                                         │  │
│  │  - 告警管理API                                                         │  │
│  │  - 售后任务API                                                         │  │
│  │  - 工作人员监控API                                                     │  │
│  │  - 用户满意度API                                                       │  │
│  │  - AI分析流程                                                         │  │
│  │  - 消息接收流程                                                       │  │
│  │  - 消息发送流程                                                       │  │
│  │  - 告警系统流程                                                       │  │
│  │  - 腾讯文档对接                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  前端测试                                                             │  │
│  │  - Dashboard页面                                                      │  │
│  │  - 会话管理页面                                                       │  │
│  │  - 告警管理页面                                                       │  │
│  │  - 售后任务页面                                                       │  │
│  │  - 工作人员管理页面                                                   │  │
│  │  - 用户管理页面                                                       │  │
│  │  - 机器人管理页面                                                     │  │
│  │  - 系统设置页面                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 测试策略

### 2.1 测试分层

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              测试分层                                        │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  单元测试 (Unit Tests)                                                 │  │
│  │  - 测试目标：单个函数、类、组件                                        │  │
│  │  - 测试工具：Jest、Vitest                                              │  │
│  │  - 测试覆盖率：≥ 80%                                                  │  │
│  │  - 执行频率：每次提交                                                  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  集成测试 (Integration Tests)                                          │  │
│  │  - 测试目标：模块间集成、API集成                                       │  │
│  │  - 测试工具：Jest、Supertest                                           │  │
│  │  - 测试覆盖率：≥ 70%                                                  │  │
│  │  - 执行频率：每次提交、每日                                            │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  端到端测试 (E2E Tests)                                               │  │
│  │  - 测试目标：完整用户流程                                              │  │
│  │  - 测试工具：Playwright                                                │  │
│  │  - 测试覆盖率：关键流程 100%                                          │  │
│  │  - 执行频率：每次发布、每周                                            │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 测试工具

```
┌──────────────────┬────────────────┬────────────────┬────────────────┐
│    测试工具       │    测试类型     │    用途        │    适用范围     │
├──────────────────┼────────────────┼────────────────┼────────────────┤
│ Jest             │ 单元测试        | 测试JS/TS代码   | 后端、前端     │
│ Vitest           │ 单元测试        | 快速单元测试    | 前端          │
│ Playwright       │ E2E测试         | 端到端测试      | 前端          │
│ Supertest        | 集成测试        | API测试        | 后端          │
│ Postman          | API测试         | API接口测试    | 后端          │
│ Artillery        | 性能测试        | 压力测试        | 后端          │
└──────────────────┴────────────────┴────────────────┴────────────────┘
```

---

## 3. 测试用例

### 3.1 后端API测试用例

#### 用户管理API

```typescript
// tests/api/users.test.ts
import { describe, it, expect, beforeAll, afterAll } from '@jest/globals';
import request from 'supertest';
import { app } from '../../server/app';

describe('用户管理API', () => {
  let authToken: string;
  let userId: string;

  beforeAll(async () => {
    // 登录获取token
    const response = await request(app)
      .post('/api/auth/login')
      .send({ username: 'admin', password: 'admin123' });
    authToken = response.body.token;
  });

  describe('GET /api/users', () => {
    it('应该返回用户列表', async () => {
      const response = await request(app)
        .get('/api/users')
        .set('Authorization', `Bearer ${authToken}`);

      expect(response.status).toBe(200);
      expect(response.body).toHaveProperty('list');
      expect(response.body).toHaveProperty('page');
      expect(response.body).toHaveProperty('pageSize');
      expect(Array.isArray(response.body.list)).toBe(true);
    });

    it('应该支持分页', async () => {
      const response = await request(app)
        .get('/api/users?page=1&pageSize=10')
        .set('Authorization', `Bearer ${authToken}`);

      expect(response.status).toBe(200);
      expect(response.body.page).toBe(1);
      expect(response.body.pageSize).toBe(10);
      expect(response.body.list.length).toBeLessThanOrEqual(10);
    });

    it('应该支持搜索', async () => {
      const response = await request(app)
        .get('/api/users?search=admin')
        .set('Authorization', `Bearer ${authToken}`);

      expect(response.status).toBe(200);
      expect(response.body.list.every((user: any) =>
        user.username.includes('admin')
      )).toBe(true);
    });
  });

  describe('POST /api/users', () => {
    it('应该创建新用户', async () => {
      const newUser = {
        username: 'testuser',
        email: 'test@example.com',
        password: 'password123',
        realName: 'Test User',
      };

      const response = await request(app)
        .post('/api/users')
        .set('Authorization', `Bearer ${authToken}`)
        .send(newUser);

      expect(response.status).toBe(201);
      expect(response.body).toHaveProperty('id');
      expect(response.body.username).toBe(newUser.username);
      expect(response.body.email).toBe(newUser.email);

      userId = response.body.id;
    });

    it('应该拒绝重复的用户名', async () => {
      const duplicateUser = {
        username: 'admin',
        email: 'admin2@example.com',
        password: 'password123',
        realName: 'Admin User 2',
      };

      const response = await request(app)
        .post('/api/users')
        .set('Authorization', `Bearer ${authToken}`)
        .send(duplicateUser);

      expect(response.status).toBe(400);
      expect(response.body).toHaveProperty('error');
    });

    it('应该验证必填字段', async () => {
      const invalidUser = {
        username: 'testuser2',
      };

      const response = await request(app)
        .post('/api/users')
        .set('Authorization', `Bearer ${authToken}`)
        .send(invalidUser);

      expect(response.status).toBe(400);
    });
  });

  describe('PUT /api/users/:id', () => {
    it('应该更新用户', async () => {
      const updates = {
        realName: 'Updated Test User',
        email: 'updated@example.com',
      };

      const response = await request(app)
        .put(`/api/users/${userId}`)
        .set('Authorization', `Bearer ${authToken}`)
        .send(updates);

      expect(response.status).toBe(200);
      expect(response.body.realName).toBe(updates.realName);
      expect(response.body.email).toBe(updates.email);
    });
  });

  describe('DELETE /api/users/:id', () => {
    it('应该删除用户', async () => {
      const response = await request(app)
        .delete(`/api/users/${userId}`)
        .set('Authorization', `Bearer ${authToken}`);

      expect(response.status).toBe(200);
    });
  });
});
```

#### 告警管理API

```typescript
// tests/api/alerts.test.ts
import { describe, it, expect, beforeAll } from '@jest/globals';
import request from 'supertest';
import { app } from '../../server/app';

describe('告警管理API', () => {
  let authToken: string;

  beforeAll(async () => {
    const response = await request(app)
      .post('/api/auth/login')
      .send({ username: 'admin', password: 'admin123' });
    authToken = response.body.token;
  });

  describe('POST /api/alerts', () => {
    it('应该创建告警', async () => {
      const alertData = {
        alertType: 'user_complaint',
        alertLevel: 'P0',
        sessionId: 'SES-001',
        userId: 'user-001',
        userName: 'Test User',
        groupId: 'GRP-001',
        groupName: 'Test Group',
        messageContent: '测试告警消息',
        reason: '测试告警原因',
      };

      const response = await request(app)
        .post('/api/alerts')
        .set('Authorization', `Bearer ${authToken}`)
        .send(alertData);

      expect(response.status).toBe(201);
      expect(response.body).toHaveProperty('alertId');
      expect(response.body.alertType).toBe(alertData.alertType);
      expect(response.body.alertLevel).toBe(alertData.alertLevel);
    });
  });

  describe('GET /api/alerts', () => {
    it('应该返回告警列表', async () => {
      const response = await request(app)
        .get('/api/alerts')
        .set('Authorization', `Bearer ${authToken}`);

      expect(response.status).toBe(200);
      expect(response.body).toHaveProperty('list');
      expect(Array.isArray(response.body.list)).toBe(true);
    });

    it('应该按状态筛选', async () => {
      const response = await request(app)
        .get('/api/alerts?status=created')
        .set('Authorization', `Bearer ${authToken}`);

      expect(response.status).toBe(200);
      expect(response.body.list.every((alert: any) =>
        alert.status === 'created'
      )).toBe(true);
    });

    it('应该按级别筛选', async () => {
      const response = await request(app)
        .get('/api/alerts?alertLevel=P0')
        .set('Authorization', `Bearer ${authToken}`);

      expect(response.status).toBe(200);
      expect(response.body.list.every((alert: any) =>
        alert.alertLevel === 'P0'
      )).toBe(true);
    });
  });
});
```

### 3.2 前端组件测试用例

#### Sidebar组件测试

```typescript
// tests/components/dashboard/Sidebar.test.tsx
import { describe, it, expect } from '@jest/globals';
import { render, screen } from '@testing-library/react';
import { Sidebar } from '@/components/dashboard/Sidebar';

describe('Sidebar组件', () => {
  it('应该渲染导航链接', () => {
    render(<Sidebar />);

    expect(screen.getByText('首页')).toBeInTheDocument();
    expect(screen.getByText('会话管理')).toBeInTheDocument();
    expect(screen.getByText('告警管理')).toBeInTheDocument();
    expect(screen.getByText('售后任务')).toBeInTheDocument();
    expect(screen.getByText('工作人员')).toBeInTheDocument();
    expect(screen.getByText('机器人')).toBeInTheDocument();
    expect(screen.getByText('系统设置')).toBeInTheDocument();
  });

  it('应该高亮当前激活的导航项', () => {
    const { container } = render(<Sidebar />, {
      router: { pathname: '/dashboard/sessions' },
    });

    const activeLink = container.querySelector('.bg-primary');
    expect(activeLink).toBeInTheDocument();
    expect(activeLink?.textContent).toContain('会话管理');
  });
});
```

#### StatsCard组件测试

```typescript
// tests/components/dashboard/StatsCard.test.tsx
import { describe, it, expect } from '@jest/globals';
import { render, screen } from '@testing-library/react';
import { StatsCard } from '@/components/dashboard/StatsCard';
import { Users } from 'lucide-react';

describe('StatsCard组件', () => {
  it('应该渲染标题和值', () => {
    render(
      <StatsCard
        title="今日会话"
        value="1,234"
        icon={Users}
      />
    );

    expect(screen.getByText('今日会话')).toBeInTheDocument();
    expect(screen.getByText('1,234')).toBeInTheDocument();
  });

  it('应该显示变化百分比', () => {
    render(
      <StatsCard
        title="活跃告警"
        value="23"
        change={{ value: 10, type: 'increase' }}
        icon={Users}
      />
    );

    expect(screen.getByText('+10%')).toBeInTheDocument();
  });

  it('应该显示下降变化', () => {
    render(
      <StatsCard
        title="待处理任务"
        value="45"
        change={{ value: 5, type: 'decrease' }}
        icon={Users}
      />
    );

    expect(screen.getByText('-5%')).toBeInTheDocument();
  });
});
```

### 3.3 E2E测试用例

```typescript
// tests/e2e/dashboard.spec.ts
import { test, expect } from '@playwright/test';

test.describe('Dashboard页面', () => {
  test.beforeEach(async ({ page }) => {
    // 登录
    await page.goto('http://localhost:5000/login');
    await page.fill('input[name="username"]', 'admin');
    await page.fill('input[name="password"]', 'admin123');
    await page.click('button[type="submit"]');
    await page.waitForURL('http://localhost:5000/dashboard');
  });

  test('应该显示统计卡片', async ({ page }) => {
    await expect(page.locator('text=今日会话')).toBeVisible();
    await expect(page.locator('text=活跃告警')).toBeVisible();
    await expect(page.locator('text=待处理任务')).toBeVisible();
    await expect(page.locator('text=满意度')).toBeVisible();
  });

  test('应该显示会话趋势图', async ({ page }) => {
    await expect(page.locator('text=会话趋势')).toBeVisible();
  });

  test('应该导航到会话管理页面', async ({ page }) => {
    await page.click('text=会话管理');
    await page.waitForURL('**/sessions');
    await expect(page.locator('text=会话管理')).toBeVisible();
  });

  test('应该导航到告警管理页面', async ({ page }) => {
    await page.click('text=告警管理');
    await page.waitForURL('**/alerts');
    await expect(page.locator('text=告警管理')).toBeVisible();
  });
});

test.describe('会话管理页面', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('http://localhost:5000/login');
    await page.fill('input[name="username"]', 'admin');
    await page.fill('input[name="password"]', 'admin123');
    await page.click('button[type="submit"]');
    await page.waitForURL('http://localhost:5000/dashboard');
    await page.click('text=会话管理');
    await page.waitForURL('**/sessions');
  });

  test('应该显示会话列表', async ({ page }) => {
    await expect(page.locator('.session-item')).toHaveCount.gte(1);
  });

  test('应该搜索会话', async ({ page }) => {
    await page.fill('input[placeholder="搜索用户..."]', '张三');
    await page.press('input[placeholder="搜索用户..."]', 'Enter');

    // 等待搜索结果
    await page.waitForTimeout(500);

    const sessionItems = await page.locator('.session-item').all();
    expect(sessionItems.length).toBeGreaterThan(0);
  });

  test('应该点击会话查看详情', async ({ page }) => {
    const firstSession = page.locator('.session-item').first();
    await firstSession.click();

    // 等待详情加载
    await page.waitForSelector('.session-details');

    await expect(page.locator('.session-details')).toBeVisible();
  });
});
```

---

## 4. 测试执行

### 4.1 测试命令

```json
// package.json
{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:performance": "artillery run tests/performance/load-test.yml"
  }
}
```

### 4.2 测试执行流程

```
1. 单元测试
   - 执行命令：npm run test
   - 检查覆盖率：npm run test:coverage
   - 修复失败的测试

2. 集成测试
   - 执行命令：npm run test
   - 检查API集成
   - 修复失败的测试

3. E2E测试
   - 启动应用：npm run dev
   - 执行命令：npm run test:e2e
   - 检查关键流程
   - 修复失败的测试

4. 性能测试
   - 执行命令：npm run test:performance
   - 分析性能报告
   - 优化性能问题
```

---

## 5. 测试报告

### 5.1 测试覆盖率要求

```
┌──────────────────┬────────────────┬────────────────┬────────────────┐
│    模块           │    行覆盖率     │    分支覆盖率   │    函数覆盖率   │
├──────────────────┼────────────────┼────────────────┼────────────────┤
│ 用户管理          | ≥ 80%          | ≥ 75%          | ≥ 85%          │
│ 角色权限          | ≥ 80%          | ≥ 75%          | ≥ 85%          │
│ 会话管理          | ≥ 75%          | ≥ 70%          | ≥ 80%          │
│ 告警管理          | ≥ 80%          | ≥ 75%          | ≥ 85%          │
│ 售后任务          | ≥ 75%          | ≥ 70%          | ≥ 80%          │
│ AI分析            | ≥ 70%          | ≥ 65%          | ≥ 75%          │
│ 消息接收          | ≥ 80%          | ≥ 75%          | ≥ 85%          │
│ 消息发送          | ≥ 80%          | ≥ 75%          | ≥ 85%          │
└──────────────────┴────────────────┴────────────────┴────────────────┘
```

### 5.2 测试报告模板

```
测试报告
============================================================

测试时间：2024-01-10 10:00:00
测试人员：测试团队
测试环境：开发环境

测试统计：
- 总测试用例数：100
- 通过：95
- 失败：3
- 跳过：2
- 通过率：95%

覆盖率统计：
- 行覆盖率：82%
- 分支覆盖率：78%
- 函数覆盖率：85%

失败用例：
1. tests/api/alerts.test.ts:45 - 创建告警时缺少必填字段
2. tests/components/dashboard/Sidebar.test.ts:12 - 导航高亮不正确
3. tests/e2e/dashboard.spec.ts:23 - 会话趋势图加载超时

性能测试结果：
- 平均响应时间：250ms
- 95%响应时间：500ms
- 99%响应时间：1000ms
- 并发用户数：100
- 成功率：99.5%

问题汇总：
- 1个P0问题
- 1个P1问题
- 1个P2问题

建议：
1. 修复P0问题后重新测试
2. 优化性能瓶颈
3. 补充边缘用例
```

---

**文档版本**：v1.0

**最后更新**：2024-01-10

**文档作者**：WorkTool AI 团队

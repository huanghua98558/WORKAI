
---

## 📦 子流程详细设计（续）

### 4. 图片处理流程 (02-image-processing.json)

**文件**：`server/flows/default/02-image-processing.json`
**ID**：`flow-image-processing-v6`
**版本**：`1.0.0`
**优先级**：`90`
**是否默认**：`false`

#### 职责
- 图片上传到对象存储
- 图片识别（OCR + 图像理解）
- 图片内容分析
- 基于图片生成回复

#### 节点清单

| 节点ID | 节点类型 | 节点名称 | 说明 |
|--------|---------|---------|------|
| node_start | start | 开始 | 流程开始节点 |
| node_image_upload | image_process | 图片上传 | 上传图片到对象存储 |
| node_image_recognition | image_process | 图片识别 | 图片OCR + 图像理解 |
| node_image_analyze | ai_reply_enhanced | 图片分析 | 基于图片内容分析 |
| node_ai_reply | ai_reply_enhanced | AI回复 | 基于图片生成回复 |
| node_flow_call_command | flow_call | 调用命令状态 | 调用命令状态流程 |
| node_end | end | 结束 | 流程结束节点 |

#### 节点详细属性

**1. node_image_upload（图片上传节点）**
```json
{
  "id": "node_image_upload",
  "type": "image_process",
  "data": {
    "name": "图片上传",
    "description": "上传图片到对象存储",
    "config": {
      "operation": "upload",
      "sourceUrl": "{{context.imageUrl}}",
      "bucket": "worktool-images",
      "path": "uploads/images/{{date}}/",
      "overwrite": false
    }
  }
}
```

**属性说明**：
- `operation` (string): 操作类型（upload/download/recognize），必填
- `sourceUrl` (string): 图片URL，必填
- `bucket` (string): 存储桶名称，必填
- `path` (string): 存储路径，可选
- `overwrite` (boolean): 是否覆盖，默认false

**2. node_image_recognition（图片识别节点）**
```json
{
  "id": "node_image_recognition",
  "type": "image_process",
  "data": {
    "name": "图片识别",
    "description": "图片OCR + 图像理解",
    "config": {
      "operation": "recognize",
      "imageUrl": "{{context.uploadedImageUrl}}",
      "ocrEnabled": true,
      "imageUnderstanding": true,
      "recognitionModel": "openai-vision"
    }
  }
}
```

**属性说明**：
- `ocrEnabled` (boolean): 是否启用OCR，默认true
- `imageUnderstanding` (boolean): 是否启用图像理解，默认true
- `recognitionModel` (string): 识别模型，默认openai-vision

**3. node_image_analyze（图片分析节点）**
```json
{
  "id": "node_image_analyze",
  "type": "ai_reply_enhanced",
  "data": {
    "name": "图片分析",
    "description": "基于图片内容分析",
    "config": {
      "aiModel": "gpt-4-vision-preview",
      "temperature": 0.7,
      "maxTokens": 1500,
      "imageUrl": "{{context.uploadedImageUrl}}",
      "ocrResult": "{{context.ocrResult}}",
      "imageContext": "{{context.imageContext}}",
      "analysisType": "content_analysis",
      "outputFields": ["imageCategory", "imageDescription", "detectedObjects"]
    }
  }
}
```

**4. node_ai_reply（AI回复节点）**
```json
{
  "id": "node_ai_reply",
  "type": "ai_reply_enhanced",
  "data": {
    "name": "AI回复",
    "description": "基于图片生成回复",
    "config": {
      "aiModel": "deepseek-chat",
      "temperature": 0.7,
      "maxTokens": 2000,
      "imageUrl": "{{context.uploadedImageUrl}}",
      "imageCategory": "{{context.imageCategory}}",
      "imageDescription": "{{context.imageDescription}}",
      "systemPrompt": "你是WorkTool AI的图片分析助手，根据图片内容生成有帮助的回复。",
      "responseFormat": "text"
    }
  }
}
```

---

### 5. 语音处理流程 (03-voice-processing.json)

**文件**：`server/flows/default/03-voice-processing.json`
**ID**：`flow-voice-processing-v6`
**版本**：`1.0.0`
**优先级**：`90`
**是否默认**：`false`

#### 职责
- 语音上传到对象存储
- 语音转文字（ASR）
- 调用文本处理流程
- 生成语音回复（TTS）

#### 节点清单

| 节点ID | 节点类型 | 节点名称 | 说明 |
|--------|---------|---------|------|
| node_start | start | 开始 | 流程开始节点 |
| node_voice_upload | http_request | 语音上传 | 上传语音到对象存储 |
| node_voice_to_text | http_request | 语音转文字 | ASR语音识别 |
| node_flow_call_text | flow_call | 调用文本处理 | 调用文本处理流程 |
| node_audio_generate | http_request | 生成语音 | TTS语音合成 |
| node_flow_call_command | flow_call | 调用命令状态 | 调用命令状态流程 |
| node_end | end | 结束 | 流程结束节点 |

#### 节点详细属性

**1. node_voice_upload（语音上传节点）**
```json
{
  "id": "node_voice_upload",
  "type": "http_request",
  "data": {
    "name": "语音上传",
    "description": "上传语音到对象存储",
    "config": {
      "method": "POST",
      "url": "https://api.worktool.com/storage/upload",
      "headers": {
        "Content-Type": "multipart/form-data"
      },
      "body": {
        "file": "{{context.audioUrl}}",
        "bucket": "worktool-audio",
        "path": "uploads/audio/{{date}}/"
      },
      "timeout": 30000
    }
  }
}
```

**属性说明**：
- `method` (string): HTTP方法（GET/POST/PUT/DELETE），必填
- `url` (string): 请求URL，必填
- `headers` (object): 请求头，可选
- `body` (object): 请求体，可选
- `timeout` (number): 超时时间（毫秒），默认30000

**2. node_voice_to_text（语音转文字节点）**
```json
{
  "id": "node_voice_to_text",
  "type": "http_request",
  "data": {
    "name": "语音转文字",
    "description": "ASR语音识别",
    "config": {
      "method": "POST",
      "url": "https://api.worktool.com/audio/asr",
      "headers": {
        "Content-Type": "application/json"
      },
      "body": {
        "audioUrl": "{{context.uploadedAudioUrl}}",
        "language": "zh-CN",
        "model": "whisper-large-v3"
      },
      "timeout": 60000
    }
  }
}
```

**3. node_flow_call_text（调用文本处理流程节点）**
```json
{
  "id": "node_flow_call_text",
  "type": "flow_call",
  "data": {
    "name": "调用文本处理",
    "description": "调用文本处理流程",
    "config": {
      "flowId": "flow-text-processing-v6",
      "version": "1.0.0",
      "timeout": 60000,
      "retryCount": 2,
      "async": false,
      "params": {
        "messageId": "{{context.messageId}}",
        "messageContent": "{{context.transcribedText}}",
        "sessionId": "{{context.sessionId}}",
        "userId": "{{context.userId}}"
      },
      "responseMapping": {
        "replyText": "context.replyText"
      }
    }
  }
}
```

**4. node_audio_generate（生成语音节点）**
```json
{
  "id": "node_audio_generate",
  "type": "http_request",
  "data": {
    "name": "生成语音",
    "description": "TTS语音合成",
    "config": {
      "method": "POST",
      "url": "https://api.worktool.com/audio/tts",
      "headers": {
        "Content-Type": "application/json"
      },
      "body": {
        "text": "{{context.replyText}}",
        "voice": "zh-CN-XiaoxiaoNeural",
        "speed": 1.0,
        "format": "mp3"
      },
      "timeout": 30000
    }
  }
}
```

---

### 6. 告警处理流程 (04-alert-handling.json)

**文件**：`server/flows/default/04-alert-handling.json`
**ID**：`flow-alert-handling-v6`
**版本**：`1.0.0`
**优先级**：`100`
**是否默认**：`false`

#### 职责
- 告警规则评估
- 告警记录保存
- 告警通知
- 告警升级循环

#### 节点清单

| 节点ID | 节点类型 | 节点名称 | 说明 |
|--------|---------|---------|------|
| node_start | start | 开始 | 流程开始节点 |
| node_alert_rule_evaluate | alert_rule | 告警规则评估 | 评估告警规则 |
| node_alert_check | decision | 告警检查 | 检查是否需要告警 |
| node_alert_save | alert_save | 告警保存 | 保存告警记录 |
| node_alert_notify | alert_notify | 告警通知 | 发送告警通知 |
| node_alert_escalation_check | decision | 升级检查 | 检查是否需要升级 |
| node_alert_escalate | alert_escalate | 告警升级 | 告警升级 |
| node_log_save | log_save | 日志记录 | 记录告警日志 |
| node_end | end | 结束 | 流程结束节点 |

#### 节点详细属性

**1. node_alert_rule_evaluate（告警规则评估节点）**
```json
{
  "id": "node_alert_rule_evaluate",
  "type": "alert_rule",
  "data": {
    "name": "告警规则评估",
    "description": "评估告警规则",
    "config": {
      "rules": [
        {
          "level": "P0",
          "condition": "context.emotion === 'extremely_negative' || context.riskLevel >= 5 || context.intent === 'complaint'",
          "timeout": 300000
        },
        {
          "level": "P1",
          "condition": "context.emotion === 'negative' || context.riskLevel >= 3 || context.sentiment < 0.3",
          "timeout": 1800000
        },
        {
          "level": "P2",
          "condition": "context.confidence < 0.6 || context.replyRate < 0.5",
          "timeout": 7200000
        },
        {
          "level": "P3",
          "condition": "context.activityLevel === 'low' || context.chatterCount > 10",
          "timeout": 86400000
        }
      ]
    }
  }
}
```

**属性说明**：
- `rules` (array): 告警规则数组，必填
  - `level` (string): 告警级别（P0/P1/P2/P3），必填
  - `condition` (string): 触发条件表达式，必填
  - `timeout` (number): 响应超时（毫秒），必填

**2. node_alert_check（告警检查节点）**
```json
{
  "id": "node_alert_check",
  "type": "decision",
  "data": {
    "name": "告警检查",
    "description": "检查是否需要告警",
    "config": {
      "expression": "context.alertLevel",
      "conditions": [
        {
          "label": "需要告警",
          "expression": "context.alertLevel !== undefined && context.alertLevel !== null",
          "targetNodeId": "node_alert_save"
        },
        {
          "label": "不需要告警",
          "expression": "true",
          "targetNodeId": "node_log_save"
        }
      ]
    }
  }
}
```

**3. node_alert_save（告警保存节点）**
```json
{
  "id": "node_alert_save",
  "type": "alert_save",
  "data": {
    "name": "告警保存",
    "description": "保存告警记录",
    "config": {
      "alertLevel": "{{context.alertLevel}}",
      "alertType": "{{context.alertType}}",
      "description": "{{context.alertDescription}}",
      "userId": "{{context.userId}}",
      "groupId": "{{context.groupId}}",
      "sessionId": "{{context.sessionId}}",
      "emotion": "{{context.emotion}}",
      "riskLevel": "{{context.riskLevel}}",
      "duplicateCheck": true,
      "duplicateInterval": 300000
    }
  }
}
```

**属性说明**：
- `alertLevel` (string): 告警级别，必填
- `alertType` (string): 告警类型，必填
- `description` (string): 告警描述，必填
- `userId` (string): 用户ID，可选
- `groupId` (string): 群组ID，可选
- `sessionId` (string): 会话ID，可选
- `emotion` (string): 情感状态，可选
- `riskLevel` (number): 风险等级，可选
- `duplicateCheck` (boolean): 是否去重检查，默认true
- `duplicateInterval` (number): 去重间隔（毫秒），默认300000

**4. node_alert_notify（告警通知节点）**
```json
{
  "id": "node_alert_notify",
  "type": "alert_notify",
  "data": {
    "name": "告警通知",
    "description": "发送告警通知",
    "config": {
      "alertLevel": "{{context.alertLevel}}",
      "notifyChannels": ["robot", "sse", "webhook"],
      "recipients": {
        "P0": ["售后主管", "运营主管"],
        "P1": ["售后主管"],
        "P2": [],
        "P3": []
      },
      "message": "告警：{{context.alertType}} - {{context.description}}",
      "pushToDashboard": true
    }
  }
}
```

**属性说明**：
- `notifyChannels` (array): 通知渠道（robot/sse/webhook/email/sms），必填
- `recipients` (object): 收件人映射，必填
- `message` (string): 通知消息，必填
- `pushToDashboard` (boolean): 是否推送到仪表盘，默认true

**5. node_alert_escalation_check（升级检查节点）**
```json
{
  "id": "node_alert_escalation_check",
  "type": "decision",
  "data": {
    "name": "升级检查",
    "description": "检查是否需要升级",
    "config": {
      "expression": "context.enableEscalation === true && context.escalationLevel < context.maxEscalation",
      "conditions": [
        {
          "label": "需要升级",
          "expression": "context.enableEscalation === true && context.escalationLevel < context.maxEscalation && (Date.now() - context.escalationStartTime >= context.escalationInterval)",
          "targetNodeId": "node_alert_escalate"
        },
        {
          "label": "不需要升级",
          "expression": "true",
          "targetNodeId": "node_log_save"
        }
      ]
    }
  }
}
```

**6. node_alert_escalate（告警升级节点）**
```json
{
  "id": "node_alert_escalate",
  "type": "alert_escalate",
  "data": {
    "name": "告警升级",
    "description": "告警升级",
    "config": {
      "alertId": "{{context.alertId}}",
      "escalateTo": "{{context.nextEscalationLevel}}",
      "escalateCondition": "时间超过{{context.escalationInterval}}或情绪恶化",
      "notifyRecipients": "{{context.escalationRecipients}}",
      "maxEscalationLevel": 5
    }
  }
}
```

**属性说明**：
- `alertId` (string): 告警ID，必填
- `escalateTo` (string): 升级目标级别，必填
- `escalateCondition` (string): 升级条件，必填
- `notifyRecipients` (array): 通知收件人，必填
- `maxEscalationLevel` (number): 最大升级级别，默认5

---

### 7. 售后处理流程 (05-after-sales.json)

**文件**：`server/flows/default/05-after-sales.json`
**ID**：`flow-after-sales-v6`
**版本**：`1.0.0`
**优先级**：`95`
**是否默认**：`false`

#### 职责
- 售后任务识别
- 创建售后任务
- 任务分配
- 任务监控
- 自动提醒
- 任务完成确认

#### 节点清单

| 节点ID | 节点类型 | 节点名称 | 说明 |
|--------|---------|---------|------|
| node_start | start | 开始 | 流程开始节点 |
| node_task_detect | decision | 任务检测 | 检测售后任务 |
| node_task_create | task_assign | 任务创建 | 创建售后任务 |
| node_task_assign | task_assign | 任务分配 | 分配任务给售后人员 |
| node_flow_call_collaboration | flow_call | 调用协同流程 | 调用协同分析流程 |
| node_task_monitor | delay | 任务监控 | 监控任务进度 |
| node_timeout_check | decision | 超时检查 | 检查是否超时 |
| node_notification | notification | 自动提醒 | 自动提醒售后人员 |
| node_completion_check | decision | 完成检查 | 检查任务是否完成 |
| node_task_complete | task_assign | 任务完成 | 标记任务完成 |
| node_flow_call_command | flow_call | 调用命令状态 | 调用命令状态流程 |
| node_end | end | 结束 | 流程结束节点 |

#### 节点详细属性

**1. node_task_detect（任务检测节点）**
```json
{
  "id": "node_task_detect",
  "type": "decision",
  "data": {
    "name": "任务检测",
    "description": "检测售后任务",
    "config": {
      "expression": "context.atMentioned === true || context.hasKeywords === true || context.ownerResponded === true",
      "conditions": [
        {
          "label": "检测到任务",
          "expression": "context.atMentioned === true || context.hasKeywords === true || context.ownerResponded === true",
          "targetNodeId": "node_task_create"
        },
        {
          "label": "无任务",
          "expression": "true",
          "targetNodeId": "node_end"
        }
      ]
    }
  }
}
```

**2. node_task_create（任务创建节点）**
```json
{
  "id": "node_task_create",
  "type": "task_assign",
  "data": {
    "name": "任务创建",
    "description": "创建售后任务",
    "config": {
      "operation": "create",
      "taskType": "{{context.taskType}}",
      "taskPriority": "normal",
      "userId": "{{context.userId}}",
      "groupId": "{{context.groupId}}",
      "sessionId": "{{context.sessionId}}",
      "staffId": "{{context.staffId}}",
      "description": "{{context.taskDescription}}",
      "estimatedCompletion": 3600000,
      "autoAssign": true
    }
  }
}
```

**属性说明**：
- `operation` (string): 操作类型（create/assign/update/complete），必填
- `taskType` (string): 任务类型（realname/scan/bind/appeal），必填
- `taskPriority` (string): 任务优先级（low/normal/high/emergency），默认normal
- `userId` (string): 用户ID，必填
- `groupId` (string): 群组ID，必填
- `sessionId` (string): 会话ID，可选
- `staffId` (string): 员工ID，可选
- `description` (string): 任务描述，必填
- `estimatedCompletion` (number): 预计完成时间（毫秒），默认3600000
- `autoAssign` (boolean): 是否自动分配，默认true

**3. node_task_assign（任务分配节点）**
```json
{
  "id": "node_task_assign",
  "type": "task_assign",
  "data": {
    "name": "任务分配",
    "description": "分配任务给售后人员",
    "config": {
      "operation": "assign",
      "taskId": "{{context.taskId}}",
      "assignTo": "{{context.staffId}}",
      "assignStrategy": "load_balance"
    }
  }
}
```

**属性说明**：
- `assignStrategy` (string): 分配策略（manual/auto/load_balance），默认auto

**4. node_task_monitor（任务监控节点）**
```json
{
  "id": "node_task_monitor",
  "type": "delay",
  "data": {
    "name": "任务监控",
    "description": "监控任务进度",
    "config": {
      "delayMode": "fixed",
      "delayTime": 60000,
      "checkCondition": "context.taskStatus !== 'completed'",
      "maxRetries": 30,
      "onTimeout": "continue"
    }
  }
}
```

**5. node_timeout_check（超时检查节点）**
```json
{
  "id": "node_timeout_check",
  "type": "decision",
  "data": {
    "name": "超时检查",
    "description": "检查是否超时",
    "config": {
      "expression": "(Date.now() - context.taskCreatedTime) > context.taskTimeout",
      "conditions": [
        {
          "label": "已超时",
          "expression": "(Date.now() - context.taskCreatedTime) > context.taskTimeout",
          "targetNodeId": "node_notification"
        },
        {
          "label": "未超时",
          "expression": "true",
          "targetNodeId": "node_completion_check"
        }
      ]
    }
  }
}
```

**6. node_notification（自动提醒节点）**
```json
{
  "id": "node_notification",
  "type": "notification",
  "data": {
    "name": "自动提醒",
    "description": "自动提醒售后人员",
    "config": {
      "notifyChannels": ["robot", "sse"],
      "recipients": ["{{context.staffId}}"],
      "message": "任务提醒：{{context.taskType}} 任务已超时，请及时处理",
      "priority": "high"
    }
  }
}
```

**属性说明**：
- `priority` (string): 优先级（low/normal/high/emergency），默认normal

**7. node_completion_check（完成检查节点）**
```json
{
  "id": "node_completion_check",
  "type": "decision",
  "data": {
    "name": "完成检查",
    "description": "检查任务是否完成",
    "config": {
      "expression": "context.taskStatus === 'completed'",
      "conditions": [
        {
          "label": "任务完成",
          "expression": "context.taskStatus === 'completed'",
          "targetNodeId": "node_task_complete"
        },
        {
          "label": "任务未完成",
          "expression": "true",
          "targetNodeId": "node_task_monitor"
        }
      ]
    }
  }
}
```

**8. node_task_complete（任务完成节点）**
```json
{
  "id": "node_task_complete",
  "type": "task_assign",
  "data": {
    "name": "任务完成",
    "description": "标记任务完成",
    "config": {
      "operation": "complete",
      "taskId": "{{context.taskId}}",
      "completionNote": "{{context.completionNote}}",
      "updateUserStatus": true
    }
  }
}
```

---

### 8. 人工转接流程 (06-staff-intervention.json)

**文件**：`server/flows/default/06-staff-intervention.json`
**ID**：`flow-staff-intervention-v6`
**版本**：`1.0.0`
**优先级**：`95`
**是否默认**：`false`

#### 职责
- 识别需要人工处理的场景
- 匹配合适的客服人员
- 转移会话
- 通知客服接管

#### 节点清单

| 节点ID | 节点类型 | 节点名称 | 说明 |
|--------|---------|---------|------|
| node_start | start | 开始 | 流程开始节点 |
| node_staff_match | staff_intervention | 员工匹配 | 匹配客服人员 |
| node_session_transfer | staff_intervention | 会话转移 | 转移会话 |
| node_notification | notification | 通知客服 | 通知客服接管 |
| node_flow_call_command | flow_call | 调用命令状态 | 调用命令状态流程 |
| node_end | end | 结束 | 流程结束节点 |

#### 节点详细属性

**1. node_staff_match（员工匹配节点）**
```json
{
  "id": "node_staff_match",
  "type": "staff_intervention",
  "data": {
    "name": "员工匹配",
    "description": "匹配客服人员",
    "config": {
      "operation": "match",
      "matchStrategy": "load_balance",
      "skillRequirements": ["售后", "客服"],
      "maxLoad": 5,
      "priorityStaff": []
    }
  }
}
```

**属性说明**：
- `operation` (string): 操作类型（match/transfer/notify），必填
- `matchStrategy` (string): 匹配策略（round_robin/load_balance/priority/skill_based），默认round_robin
- `skillRequirements` (array): 技能要求，可选
- `maxLoad` (number): 最大负载，默认5
- `priorityStaff` (array): 优先员工列表，可选

**2. node_session_transfer（会话转移节点）**
```json
{
  "id": "node_session_transfer",
  "type": "staff_intervention",
  "data": {
    "name": "会话转移",
    "description": "转移会话",
    "config": {
      "operation": "transfer",
      "sessionId": "{{context.sessionId}}",
      "transferTo": "{{context.matchedStaffId}}",
      "transferNote": "{{context.transferReason}}",
      "notifySender": true,
      "notifyRecipient": true
    }
  }
}
```

**属性说明**：
- `sessionId` (string): 会话ID，必填
- `transferTo` (string): 转移目标员工ID，必填
- `transferNote` (string): 转移说明，可选
- `notifySender` (boolean): 是否通知发送者，默认true
- `notifyRecipient` (boolean): 是否通知接收者，默认true

---

### 9. 协同分析流程 (07-collaboration-analysis.json)

**文件**：`server/flows/default/07-collaboration-analysis.json`
**ID**：`flow-collaboration-analysis-v6`
**版本**：`1.0.0`
**优先级**：`80`
**是否默认**：`false`

#### 职责
- 人工活跃度分析
- 用户满意度分析
- 工作人员满意度分析
- 售后任务追踪
- 运营语气监测
- 介入决策
- 生成协同分析报告

#### 节点清单

| 节点ID | 节点类型 | 节点名称 | 说明 |
|--------|---------|---------|------|
| node_start | start | 开始 | 流程开始节点 |
| node_human_activity_analyze | collaboration_analyze | 人工活跃度分析 | 分析人工活跃度 |
| node_user_satisfaction_analyze | satisfaction_infer | 用户满意度分析 | 分析用户满意度 |
| node_staff_satisfaction_analyze | collaboration_analyze | 员工满意度分析 | 分析员工满意度 |
| node_task_tracking | task_assign | 任务追踪 | 追踪售后任务 |
| node_operator_tone_analyze | collaboration_analyze | 运营语气分析 | 分析运营语气 |
| node_intervention_decision | decision | 介入决策 | 决定是否介入 |
| node_intervention_action | staff_intervention | 介入行动 | 执行介入行动 |
| node_report_generate | data_transform | 报告生成 | 生成协同分析报告 |
| node_end | end | 结束 | 流程结束节点 |

#### 节点详细属性

**1. node_human_activity_analyze（人工活跃度分析节点）**
```json
{
  "id": "node_human_activity_analyze",
  "type": "collaboration_analyze",
  "data": {
    "name": "人工活跃度分析",
    "description": "分析人工活跃度",
    "config": {
      "analyzeType": "human_activity",
      "metrics": [
        "online_status",
        "reply_frequency",
        "response_time",
        "reply_quality"
      ],
      "timeRange": 3600000,
      "sessionId": "{{context.sessionId}}"
    }
  }
}
```

**属性说明**：
- `analyzeType` (string): 分析类型（human_activity/user_satisfaction/staff_satisfaction/task_tracking/operator_tone），必填
- `metrics` (array): 分析指标列表，必填
- `timeRange` (number): 时间范围（毫秒），默认3600000
- `sessionId` (string): 会话ID，可选

**2. node_user_satisfaction_analyze（用户满意度分析节点）**
```json
{
  "id": "node_user_satisfaction_analyze",
  "type": "satisfaction_infer",
  "data": {
    "name": "用户满意度分析",
    "description": "分析用户满意度",
    "config": {
      "analyzeType": "user_satisfaction",
      "metrics": [
        "sentiment_analysis",
        "keyword_extraction",
        "satisfaction_score"
      ],
      "historyLimit": 50,
      "userId": "{{context.userId}}"
    }
  }
}
```

**属性说明**：
- `analyzeType` (string): 分析类型（user_satisfaction/staff_satisfaction），必填
- `historyLimit` (number): 历史消息数量，默认50

**3. node_staff_satisfaction_analyze（员工满意度分析节点）**
```json
{
  "id": "node_staff_satisfaction_analyze",
  "type": "collaboration_analyze",
  "data": {
    "name": "员工满意度分析",
    "description": "分析员工满意度",
    "config": {
      "analyzeType": "staff_satisfaction",
      "metrics": [
        "feedback_analysis",
        "resolution_rate",
        "handling_time"
      ],
      "staffId": "{{context.staffId}}",
      "timeRange": 86400000
    }
  }
}
```

**4. node_task_tracking（任务追踪节点）**
```json
{
  "id": "node_task_tracking",
  "type": "task_assign",
  "data": {
    "name": "任务追踪",
    "description": "追踪售后任务",
    "config": {
      "operation": "track",
      "userId": "{{context.userId}}",
      "groupId": "{{context.groupId}}",
      "sessionId": "{{context.sessionId}}"
    }
  }
}
```

**5. node_operator_tone_analyze（运营语气分析节点）**
```json
{
  "id": "node_operator_tone_analyze",
  "type": "collaboration_analyze",
  "data": {
    "name": "运营语气分析",
    "description": "分析运营语气",
    "config": {
      "analyzeType": "operator_tone",
      "metrics": [
        "tone_analysis",
        "owner_cooperation",
        "intervention_strategy"
      ],
      "operatorId": "{{context.operatorId}}",
      "timeRange": 3600000
    }
  }
}
```

**6. node_intervention_decision（介入决策节点）**
```json
{
  "id": "node_intervention_decision",
  "type": "decision",
  "data": {
    "name": "介入决策",
    "description": "决定是否介入",
    "config": {
      "expression": "context.needIntervention === true",
      "conditions": [
        {
          "label": "需要介入",
          "expression": "context.needIntervention === true",
          "targetNodeId": "node_intervention_action"
        },
        {
          "label": "不需要介入",
          "expression": "true",
          "targetNodeId": "node_report_generate"
        }
      ]
    }
  }
}
```

**7. node_intervention_action（介入行动节点）**
```json
{
  "id": "node_intervention_action",
  "type": "staff_intervention",
  "data": {
    "name": "介入行动",
    "description": "执行介入行动",
    "config": {
      "operation": "intervene",
      "interventionType": "{{context.interventionType}}",
      "targetUserId": "{{context.userId}}",
      "interventionMessage": "{{context.interventionMessage}}"
    }
  }
}
```

**属性说明**：
- `interventionType` (string): 介入类型（apologize/assist/escalate），必填
- `targetUserId` (string): 目标用户ID，必填
- `interventionMessage` (string): 介入消息，必填

**8. node_report_generate（报告生成节点）**
```json
{
  "id": "node_report_generate",
  "type": "data_transform",
  "data": {
    "name": "报告生成",
    "description": "生成协同分析报告",
    "config": {
      "transformType": "report",
      "reportType": "collaboration_analysis",
      "includeSections": [
        "human_activity",
        "user_satisfaction",
        "staff_satisfaction",
        "task_tracking",
        "operator_tone",
        "intervention_summary"
      ],
      "outputFormat": "json",
      "saveToDatabase": true
    }
  }
}
```

**属性说明**：
- `transformType` (string): 转换类型（report/aggregate/filter/merge），必填
- `reportType` (string): 报告类型，必填
- `includeSections` (array): 包含的章节列表，必填
- `outputFormat` (string): 输出格式（json/html/pdf），默认json
- `saveToDatabase` (boolean): 是否保存到数据库，默认true

---

## 🎯 总结

### 流程清单（12个）

1. ✅ **00-main-flow.json** - 主流程（调度器）
2. ✅ **01-text-processing.json** - 文本处理流程
3. ✅ **02-image-processing.json** - 图片处理流程
4. ✅ **03-voice-processing.json** - 语音处理流程
5. ✅ **04-alert-handling.json** - 告警处理流程
6. ✅ **05-after-sales.json** - 售后处理流程
7. ✅ **06-staff-intervention.json** - 人工转接流程
8. ✅ **07-collaboration-analysis.json** - 协同分析流程
9. ✅ **08-command-status.json** - 命令状态流程
10. ✅ **09-community-analysis.json** - 社群数据分析流程
11. ✅ **10-after-sales-analysis.json** - 售后数据分析流程
12. ✅ **11-operator-maintenance.json** - 运营维护流程

### 节点类型（40种）

基础节点（4种）：
- start, end, decision, condition

消息处理节点（3种）：
- message_receive, session_create, context_enhancer

AI处理节点（5种）：
- unified_analyze, ai_reply, ai_reply_enhanced, emotion_analyze, intent

流程控制节点（2种）：
- flow_call, delay

机器人交互节点（2种）：
- robot_dispatch, send_command

命令状态节点（1种）：
- command_status

告警系统节点（4种）：
- alert_rule, alert_save, alert_notify, alert_escalate

任务管理节点（1种）：
- task_assign

协同分析节点（1种）：
- collaboration_analyze

人工干预节点（1种）：
- staff_intervention

满意度推断节点（1种）：
- satisfaction_infer

数据转换节点（1种）：
- data_transform

通知节点（1种）：
- notification

日志节点（1种）：
- log_save

HTTP请求节点（1种）：
- http_request

图片处理节点（1种）：
- image_process

变量设置节点（1种）：
- variable_set

数据查询节点（1种）：
- data_query

风险检测节点（1种）：
- risk_detect

智能分析节点（1种）：
- smart_analyze

工作人员消息节点（1种）：
- staff_message

消息分发节点（1种）：
- message_dispatch

消息同步节点（1种）：
- message_sync

风险处理节点（1种）：
- risk_handler

监控节点（1种）：
- monitor

服务节点（1种）：
- service

人工转接节点（1种）：
- human_handover

AI对话节点（1种）：
- ai_chat

### 属性验证

✅ 所有节点都有详细的属性定义
✅ 所有属性都有默认值
✅ 所有可选属性都有说明
✅ 所有必填属性都标注了
✅ 属性丢失影响都有说明

---

**文档版本**：v6.0.0
**最后更新**：2025-01-10
**状态**：✅ 设计完成，待实施
